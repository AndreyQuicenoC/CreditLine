[pytest]
DJANGO_SETTINGS_MODULE = creditline.settings
python_files = tests.py test_*.py *_tests.py
testpaths = tests
addopts = 
    -v
    --tb=short
    --strict-markers
    --ds=creditline.settings

markers =
    django_db: mark test to use the Django database
    integration: mark test as an integration test
